# Ralph Progress — Plugboard Call Reduction & Observability (master T275074754)

One subtask per iteration. Pick the lowest-numbered incomplete subtask whose deps are complete.

| Subtask | Plan task | Deps | Status | sl commit | Notes |
|---------|-----------|------|--------|-----------|-------|
| S1 T275074837 auto_drop config gate | T1 | — | DONE | (pending) | 21 pipeline tests pass; ruff unavailable in env |
| S2 T275074853 main-app observability | T2-3 | — | DONE | (pending) | shim + plugboard_* metrics + sink wiring; 8 llm/scorer + 7 metrics/shim tests pass |
| S6 T275074867 memory-service observability | T7 | — | DONE | (pending) | metrics.py + instrument_llm_client + /metrics + MetricsConfig + episode metrics + summary; 33 memory tests pass. Used in-place instance wrapping (not subclass) — lazy/testable; real-graphiti token capture pending graphiti env |
| S3 T275074882 periodic usage summary | T4 | S2 | DONE | (pending) | UsageAggregator + llm_usage_summary task; sink feeds aggregator; 35 related tests pass |
| S4 T275074896 batch score_relevance | T5 | S2 | DONE | (pending) | score_relevance_many + decide_from_score + PipelineConfig thresholds wired + engine batch path; 40 tests pass |
| S5 T275074915 batch score_urgency | T6 | S2 | DONE | (pending) | score_urgency_many (max_tokens scales) + enqueue urgency_score param + both scheduler paths batched; 46 tests pass |
| S7 T275074941 ops wiring | T8 | S2,S4,S5,S6 | todo | — | ready-for-human (author only) |

## Log
- iter 6: validated S4+S6 (on disk). S5 done — queue_scorer/llm.py: score_urgency_many(items, max_batch_size) with indexed prompt, max_tokens=min(4096,40*n+100) (fixes 100-cap blocker), per-item fallback, chunking, routed through shim (client=queue_scorer). engine.enqueue: optional urgency_score param (skips internal scorer when provided; dedup preserved). scheduler.py: new _enqueue_with_urgency helper pre-scores the signalled subset in ONE score_urgency_many call (gated on batching.enabled+score_urgency) then enqueues each with urgency_score; both paths refactored (_poll_one_source non-monitoring branch + _route_poll_results defers new items to a batched pass). test_batching score_urgency tests (3) + 46 related tests pass (incl. scheduler tests in test_pipeline).
- iter 5: validated S1,S2,S3,S6 (on disk). S4 done — filter.py: extracted decide_from_score(...) + gather_facts_and_rules(...); score_and_decide reuses both. anthropic.py: score_relevance_many(contexts, max_batch_size) with indexed multi-item prompt, per-item + whole-batch fallback, chunking. config.py: BatchingConfig (enabled/score_relevance/score_urgency/max_batch_size=20) + AppConfig.batching. engine.py: __init__ gains include/drop/confidence_threshold + batch_relevance + max_batch_size; process_raw_item gathers per-item facts via asyncio.gather + one score_relevance_many call when batch_relevance, routes each via _process_extracted_item(precomputed); _process_extracted_item uses filter.decide_from_score with engine thresholds when precomputed, else score_and_decide. main.py wires PipelineConfig thresholds + batching into PipelineEngine. instrumentation.py proxies score_relevance_many. DEVIATION: score_relevance_many takes per-item (item,facts,rules) contexts (spec §3.2 per-item facts) instead of the plan test's shared-facts signature. test_batching.py (5) + 40 related tests pass.
- iter 4: validated S1-S3 (on disk). S6 done — memory/metrics.py (plugboard_* client=memory + memory_episodes_ingested_total + gauges); memory/instrumentation.py (instrument_llm_client wraps the client's generate_response in place — type-preserving, no graphiti import — + MemoryUsageAggregator); memory/config.py MetricsConfig + MemoryConfig.metrics; main.py instruments llm_client in lifespan, passes metrics to worker (episodes_ingested triage/decision), increments type=entity in record_entity handler, adds unauthenticated /metrics, runs a memory summary task; prometheus-client added to memory deps. 33 memory tests pass (test_memory_metrics 4 + models/queue). DEVIATION from ADR 0050: in-place instance wrapping instead of subclass (lazy import + testability + isinstance-safe); real-graphiti token capture still best-effort, pending a graphiti-enabled env to verify the exact generate method.
- iter 3: validated S1+S2 (on disk). S3 done — new usage_aggregator.py (UsageAggregator record/drain keyed by (client,model)); MetricsConfig gains summary_log (True) + summary_interval_seconds (30); main.py sink now also feeds app.state.usage_agg; periodic _summary_loop drains every interval and logs llm_usage_summary (skip empty); task cancelled on shutdown before provider teardown. 35 related tests pass.
- iter 2: validated S1 (on disk, all wiring present). S2 done — created providers/_plugboard.py (PlugboardCallRecord + record_plugboard_call); added on_plugboard_call to AnthropicLLM + LLMQueueScorer ProviderConfig + self._sink; routed _call_with_retry (with item_count param for S4), interpret_triage_response, and score_urgency through the shim; added plugboard_* metric family (calls/errors/tokens{direction}/items + call_seconds buckets→60s) to metrics.py; wired _plugboard_sink in main.py lifespan onto app.state.llm._inner and queue_scorer, gated on config.metrics.enabled. Tests: test_plugboard_shim.py (3) + test_metrics plugboard test + test_anthropic_llm/test_queue_scorer/test_queue_worker (8) + test_pipeline (21) all green.
- iter 1: S1 done — `pipeline.record_drop_decisions` (default False) gates the auto_drop `record_pipeline_decision` call; `job.items_dropped` stays outside the gate; wired `triage_expiry_days` + `record_drop_decisions` in main.py lifespan. 2 new tests + 21 test_pipeline.py pass. Note: `ruff` not installed in venv, lint not runnable here.
