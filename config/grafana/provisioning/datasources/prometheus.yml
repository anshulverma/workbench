# Grafana datasource provisioning: point at the compose-hosted Prometheus.
# Host networking => Prometheus is reachable on localhost:9090.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://localhost:9090
    isDefault: true
    editable: true
