// Placeholder for generated OpenAPI types.
//
// This file is a hand-written stub committed so the project type-checks and
// builds even when the backend is not running. Regenerate the real types
// against a live server with:
//
//   npm run gen:api
//
// which runs `openapi-typescript http://127.0.0.1:8421/openapi.json -o src/lib/api-types.ts`.
export {}
